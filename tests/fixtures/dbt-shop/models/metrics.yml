metrics:
  - name: average_order_value
    description: Average net revenue per completed order.
    type: ratio
    label: AOV
    type_params:
      numerator:
        name: revenue
      denominator:
        name: orders

  - name: trailing_revenue
    type: cumulative
    type_params:
      measure: revenue
