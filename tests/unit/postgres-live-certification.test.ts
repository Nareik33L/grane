/**
 * Thin alias so `vitest run tests/unit` still executes the certification corpus.
 * The parameterised suite lives in `tests/certification/corpus.test.ts`.
 */
import "../certification/corpus.test.js";
