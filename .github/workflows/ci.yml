name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:unit
      # The A/B/C thesis benchmark. DuckDB is a devDependency, so this runs
      # without making it a default runtime install.
      - run: npm run test:benchmark
      # Internal robustness gauntlet. Findings are the report; this job fails
      # only when the harness is broken or a known defect-class mutation is
      # not detected. 100% is not the goal.
      - run: npm run test:gauntlet
      - run: npm run build
